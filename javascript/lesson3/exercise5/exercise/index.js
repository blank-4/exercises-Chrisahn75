function circlearea (r) {
    return Math.PI * (r *r)
}

document .write("L'air d'un cercle de rayon 3cm est " +circlearea(3) +"cm2<BR>");

function circleperimeter (p) {
    return Math.PI * (p * p)
}

document .write("Le périmetre d'un cercle de rayon 4cm est " +circleperimeter(4) +"cm<BR>");

function circlediameter (p) {
    return Math.PI * (p * p)
}

document .write("Le diametre d'un cercle de rayon 5cm est " +circlediameter(5) +"cm<BR>");